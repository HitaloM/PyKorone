interface:
  display_name: "Issue Fixer"
  short_description: "Root-cause bug fixing for PyKorone"
  default_prompt: "Use $issue-fixer to diagnose this PyKorone issue and fix its root cause when implementation is requested."
