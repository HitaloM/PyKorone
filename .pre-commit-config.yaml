repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: mixed-line-ending
      - id: check-toml
      - id: check-yaml

  - repo: local
    hooks:
      - id: update-version
        name: Update version to current date
        entry: python3 scripts/update_version.py
        language: system
        files: ^(src/korone/__init__\.py|scripts/update_version\.py)$
        pass_filenames: false

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.3
    hooks:
      - id: ruff-check
        args: [--fix, --preview, --unsafe-fixes]
      - id: ruff-format
