variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  POETRY_CACHE_DIR: "$CI_PROJECT_DIR/.cache/poetry"

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - .cache/pip
    - .cache/poetry
    - .venv

test-codestyle:
  stage: test
  image: d21d3q/python-poetry:3.13
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - .cache/pip
      - .cache/poetry
      - .venv
    policy: pull-push
  before_script:
    - poetry config virtualenvs.in-project true
    - poetry install --with dev --no-interaction
  script:
    - poetry run make test_code_style


test-codeanal:
  stage: test
  image: d21d3q/python-poetry:3.13
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - .cache/pip
      - .cache/poetry
      - .venv
    policy: pull-push
  before_script:
    - poetry config virtualenvs.in-project true
    - poetry install --with dev --no-interaction
  script:
    - poetry run make test_codeanalysis


#test-code:
#  variables:
#    TOKEN: "123456:test"
#  stage: test
#  image: d21d3q/python-poetry
#  before_script:
#    - poetry install --with dev
#    - poetry run make new_locale
#  script:
#    - poetry run make test_code
#  artifacts:
#    when: always
#    paths:
#      - allure_results
#    expire_in: 1 day
